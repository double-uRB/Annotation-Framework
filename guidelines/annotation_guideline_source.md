# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 1: Introduction & Purpose

This annotation guideline serves as the single source of truth for the FinSight AI annotation framework...

# Section 2: Document Classification Guide

## Primary Classification Decision Tree

# Section 2: Document Classification Guide

## Primary Classification Decision Tree

# Section 2: Document Classification Guide

## Primary Classification Decision Tree

# Section 2: Document Classification Guide

## Primary Classification Decision Tree

# Section 2: Document Classification Guide

## Primary Classification Decision Tree

# Section 3: Entity Type Definitions

## MONETARY_AMOUNT
**Definition:** Definition for MONETARY_AMOUNT...

**Positive Example 1:** [Sample text with MONETARY_AMOUNT] -> Normalized as {...}
**Positive Example 2:** [Sample text with MONETARY_AMOUNT] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as MONETARY_AMOUNT] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## DATE
**Definition:** Definition for DATE...

**Positive Example 1:** [Sample text with DATE] -> Normalized as {...}
**Positive Example 2:** [Sample text with DATE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as DATE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## ACCOUNT_NUMBER
**Definition:** Definition for ACCOUNT_NUMBER...

**Positive Example 1:** [Sample text with ACCOUNT_NUMBER] -> Normalized as {...}
**Positive Example 2:** [Sample text with ACCOUNT_NUMBER] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as ACCOUNT_NUMBER] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## PARTY_NAME
**Definition:** Definition for PARTY_NAME...

**Positive Example 1:** [Sample text with PARTY_NAME] -> Normalized as {...}
**Positive Example 2:** [Sample text with PARTY_NAME] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as PARTY_NAME] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## REGULATORY_ID
**Definition:** Definition for REGULATORY_ID...

**Positive Example 1:** [Sample text with REGULATORY_ID] -> Normalized as {...}
**Positive Example 2:** [Sample text with REGULATORY_ID] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as REGULATORY_ID] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## ADDRESS
**Definition:** Definition for ADDRESS...

**Positive Example 1:** [Sample text with ADDRESS] -> Normalized as {...}
**Positive Example 2:** [Sample text with ADDRESS] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as ADDRESS] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## INTEREST_RATE
**Definition:** Definition for INTEREST_RATE...

**Positive Example 1:** [Sample text with INTEREST_RATE] -> Normalized as {...}
**Positive Example 2:** [Sample text with INTEREST_RATE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as INTEREST_RATE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## DURATION
**Definition:** Definition for DURATION...

**Positive Example 1:** [Sample text with DURATION] -> Normalized as {...}
**Positive Example 2:** [Sample text with DURATION] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as DURATION] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## PERCENTAGE
**Definition:** Definition for PERCENTAGE...

**Positive Example 1:** [Sample text with PERCENTAGE] -> Normalized as {...}
**Positive Example 2:** [Sample text with PERCENTAGE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as PERCENTAGE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## DOCUMENT_ID
**Definition:** Definition for DOCUMENT_ID...

**Positive Example 1:** [Sample text with DOCUMENT_ID] -> Normalized as {...}
**Positive Example 2:** [Sample text with DOCUMENT_ID] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as DOCUMENT_ID] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## CLAUSE_REFERENCE
**Definition:** Definition for CLAUSE_REFERENCE...

**Positive Example 1:** [Sample text with CLAUSE_REFERENCE] -> Normalized as {...}
**Positive Example 2:** [Sample text with CLAUSE_REFERENCE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as CLAUSE_REFERENCE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## TRANSACTION_TYPE
**Definition:** Definition for TRANSACTION_TYPE...

**Positive Example 1:** [Sample text with TRANSACTION_TYPE] -> Normalized as {...}
**Positive Example 2:** [Sample text with TRANSACTION_TYPE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as TRANSACTION_TYPE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## INSTITUTION_NAME
**Definition:** Definition for INSTITUTION_NAME...

**Positive Example 1:** [Sample text with INSTITUTION_NAME] -> Normalized as {...}
**Positive Example 2:** [Sample text with INSTITUTION_NAME] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as INSTITUTION_NAME] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## PRODUCT_NAME
**Definition:** Definition for PRODUCT_NAME...

**Positive Example 1:** [Sample text with PRODUCT_NAME] -> Normalized as {...}
**Positive Example 2:** [Sample text with PRODUCT_NAME] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as PRODUCT_NAME] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## STATUS
**Definition:** Definition for STATUS...

**Positive Example 1:** [Sample text with STATUS] -> Normalized as {...}
**Positive Example 2:** [Sample text with STATUS] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as STATUS] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## CONTACT_INFO
**Definition:** Definition for CONTACT_INFO...

**Positive Example 1:** [Sample text with CONTACT_INFO] -> Normalized as {...}
**Positive Example 2:** [Sample text with CONTACT_INFO] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as CONTACT_INFO] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## SIGNATURE_BLOCK
**Definition:** Definition for SIGNATURE_BLOCK...

**Positive Example 1:** [Sample text with SIGNATURE_BLOCK] -> Normalized as {...}
**Positive Example 2:** [Sample text with SIGNATURE_BLOCK] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as SIGNATURE_BLOCK] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## TABLE_DATA
**Definition:** Definition for TABLE_DATA...

**Positive Example 1:** [Sample text with TABLE_DATA] -> Normalized as {...}
**Positive Example 2:** [Sample text with TABLE_DATA] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as TABLE_DATA] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## COMPUTATION
**Definition:** Definition for COMPUTATION...

**Positive Example 1:** [Sample text with COMPUTATION] -> Normalized as {...}
**Positive Example 2:** [Sample text with COMPUTATION] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as COMPUTATION] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## FOOTNOTE_REF
**Definition:** Definition for FOOTNOTE_REF...

**Positive Example 1:** [Sample text with FOOTNOTE_REF] -> Normalized as {...}
**Positive Example 2:** [Sample text with FOOTNOTE_REF] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as FOOTNOTE_REF] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## CURRENCY_CODE
**Definition:** Definition for CURRENCY_CODE...

**Positive Example 1:** [Sample text with CURRENCY_CODE] -> Normalized as {...}
**Positive Example 2:** [Sample text with CURRENCY_CODE] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as CURRENCY_CODE] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
## FISCAL_PERIOD
**Definition:** Definition for FISCAL_PERIOD...

**Positive Example 1:** [Sample text with FISCAL_PERIOD] -> Normalized as {...}
**Positive Example 2:** [Sample text with FISCAL_PERIOD] -> Normalized as {...}
**Negative Example 1:** [Sample text NOT to annotate as FISCAL_PERIOD] -> Reason: ...

This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
This entity is critical for downstream extraction. The boundaries must strictly adhere to the minimal span principle, capturing only the exact tokens representing the entity without surrounding context or labels.
# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 4: Annotation Conventions

# Section 5: Edge Case Compendium

**Edge Case 1:** Detailed resolution for edge case scenario 1...

**Edge Case 2:** Detailed resolution for edge case scenario 2...

**Edge Case 3:** Detailed resolution for edge case scenario 3...

**Edge Case 4:** Detailed resolution for edge case scenario 4...

**Edge Case 5:** Detailed resolution for edge case scenario 5...

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 6: Document-Specific Instructions

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 7: Quality Tag Assignment Guide

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 8: Common Errors & Anti-Patterns

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

# Section 9: Appendices

## Complex Multi-Entity Examples
**Complex Example 1:** Shows overlapping and nested entities with relationships.

**Complex Example 2:** Shows overlapping and nested entities with relationships.

**Complex Example 3:** Shows overlapping and nested entities with relationships.

